<div
  class="flex justify-center items-center py-10 px-4 min-h-screen bg-base-200"
>
  <div class="w-full max-w-md">
    <div class="card bg-base-100 shadow-2xl border border-base-300">
      <div
        class="bg-primary text-primary-content p-6 rounded-t-lg border-b-4 border-primary-focus"
      >
        <h3 class="text-3xl font-extrabold tracking-wide text-center">
          ĐĂNG NHẬP HỆ THỐNG 🔑
        </h3>
      </div>

      <div class="card-body p-8 space-y-6">
        <% if (typeof error !== 'undefined' && error) { %>
        <div role="alert" class="alert alert-error text-sm shadow-md">
          <svg
            xmlns="http://www.w3.org/2000/svg"
            class="stroke-current shrink-0 h-5 w-5"
            fill="none"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z"
            />
          </svg>
          <span><%= error %></span>
        </div>
        <% } %>

        <form action="/api/auth/login" method="POST" class="space-y-6">
          <div class="form-control">
            <label for="email" class="label">
              <span class="label-text font-semibold text-base-content"
                >Email</span
              >
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              class="input input-bordered w-full input-lg focus:ring-2 focus:ring-primary"
              placeholder="Nhập địa chỉ email"
            />
          </div>

          <div class="form-control">
            <label for="password" class="label">
              <span class="label-text font-semibold text-base-content"
                >Mật khẩu</span
              >
            </label>
            <input
              type="password"
              id="password"
              name="password"
              required
              class="input input-bordered w-full input-lg focus:ring-2 focus:ring-primary"
              placeholder="Nhập mật khẩu"
            />
            <label class="label justify-end">
              <a
                href="#"
                class="label-text-alt link link-hover text-sm text-info"
                >Quên mật khẩu?</a
              >
            </label>
          </div>

          <div class="form-control pt-4">
            <button
              type="submit"
              class="btn btn-primary btn-lg w-full font-bold shadow-lg hover:shadow-xl transition-shadow"
            >
              Đăng nhập
            </button>
          </div>
        </form>
      </div>

      <div
        class="card-actions justify-center p-5 border-t border-base-300 bg-base-100 rounded-b-lg"
      >
        <p class="text-base text-center text-base-content/80">
          Chưa có tài khoản?
          <a
            href="/auth/register"
            class="link link-hover link-primary font-extrabold ml-1"
            >Đăng ký ngay</a
          >
        </p>
      </div>
    </div>
  </div>
</div>
